#include "main.h"
/**
*flip_bits - set bit at given index
*@n: number
*@m: index
*Return: 1 if sucess -1 otherwise
*/
unsigned int flip_bits(unsigned long int n, unsigned long int m);
{
unsigned long int flips, i;
while (filps)
{
if (filps & 1)
i++;
filps = filps >> 1;
}
return (i);
}
